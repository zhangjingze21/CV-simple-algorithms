_target_: src.data.animateFace.FaceDataModule
data_dir: /home/stu5/scratch/dev/learning/CV/hw2-3/data/animate-face
batch_size: 129 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
train_val_test_split: [0.8, 0.1, 0.1]
num_workers: 10
pin_memory: True